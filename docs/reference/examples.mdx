---
title: "Examples"
description: "Concrete curl, JavaScript, and WebSocket examples against the current exchange."
---

## Provision a competition user

```bash
curl \
  -X POST \
  -H "Authorization: Bearer ADMIN_API_TOKEN" \
  -H "content-type: application/json" \
  https://exchange.jamesxu.dev/api/v1/admin/users \
  -d '{"username":"team-alpha"}'
```

## Fetch authenticated account data

```bash
curl -H "x-api-key: YOUR_API_KEY" \
  https://exchange.jamesxu.dev/api/v1/user

curl -H "x-api-key: YOUR_API_KEY" \
  https://exchange.jamesxu.dev/api/v1/balance

curl -H "x-api-key: YOUR_API_KEY" \
  'https://exchange.jamesxu.dev/api/v1/open-orders?market=BTC-USD'
```

## Submit an order from JavaScript

```ts
const response = await fetch("https://exchange.jamesxu.dev/api/v1/orders", {
  method: "POST",
  headers: {
    "content-type": "application/json",
    "x-api-key": process.env.EXCHANGE_API_KEY!,
  },
  body: JSON.stringify({
    market: "BTC-USD",
    side: "BUY",
    price: 100,
    quantity: 2,
  }),
});

if (!response.ok) {
  throw new Error(await response.text());
}

const payload = await response.json();
console.log(payload);
```

## WebSocket session example

```js
const socket = new WebSocket("wss://exchange.jamesxu.dev/ws");

socket.addEventListener("open", () => {
  socket.send(JSON.stringify({
    op: "authenticate",
    api_key: process.env.EXCHANGE_API_KEY,
  }));

  socket.send(JSON.stringify({
    op: "subscribe",
    channel: "l3",
    market: "BTC-USD",
  }));
});

socket.addEventListener("message", (event) => {
  const message = JSON.parse(event.data);
  console.log(message);
});
```

## Health check

```bash
curl https://exchange.jamesxu.dev/health
```

## Deployment update example

```bash
git push origin main
ssh -i "quant-exchange.pem" ec2-user@16.59.150.9 '
  cd ~/exchange-v2 &&
  git pull --ff-only &&
  cd exchange &&
  source "$HOME/.cargo/env" &&
  cargo build --release &&
  sudo systemctl restart exchange
'
```
